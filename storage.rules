rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default deny; open up specific paths as features need uploads.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
